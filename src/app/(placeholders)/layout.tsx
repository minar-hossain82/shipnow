import { PlaceholderShell } from "@/components/placeholder/placeholder-shell";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <PlaceholderShell>{children}</PlaceholderShell>;
}
